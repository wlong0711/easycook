import 'package:flutter/material.dart';

class MyPantryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("🏪 My Pantry", style: TextStyle(fontSize: 24))),
    );
  }
}
